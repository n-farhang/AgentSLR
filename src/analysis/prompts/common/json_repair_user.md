You will be given text that was intended to be a single JSON object but is invalid.
Return ONLY a valid JSON object (no markdown, no commentary).
Do not add new fields beyond those already present; only fix syntax.

TEXT:
{{RAW_TEXT}}
